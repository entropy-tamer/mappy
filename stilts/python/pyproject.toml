[project]
name = "stilts-python"
version = "0.1.0"
description = "Python bindings for Stilts tag compression library"
requires-python = ">=3.8"
dependencies = []

[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[tool.maturin]
module-name = "stilts_python"

